import Card from "../../components/Card/Card";

const Home = () => {
	return (
		<>
			<main>Test</main>
			<Card entityId={1} />
			<Card entityId={2232} />
			<Card entityId={2} />
			<Card entityId={22323} />
		</>
	);
};

export default Home;
